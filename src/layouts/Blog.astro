---
import Layout from './Main.astro';

export interface Props {
	title: string;
}

const { blogTitle } = Astro.props;
const title = `Ligature - Blog - ${blogTitle}`
---
<Layout title={title}>
  <slot />
  <a href="/">Home</a> | <a href="/blog/">Blog Articles</a>
</Layout>
