---
import Layout from './Main.astro';

export interface Props {
	title: string;
}

const { title } = Astro.props;
const fullTitle = `Ligature - Documentation - ${title}`
---
<Layout title={fullTitle}>
  <slot />
  <a href="/">Home</a> | <a href="..">Documentation</a>
</Layout>
